# BACKGROUND
background {
    monitor =
    path = ~/Pictures/wallpapers/wp2448301-hiroshi-nagai-wallpapers.jpg
    blur_passes = 2
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

# GENERAL
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = false
}

# TIME
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%-I:%M%p")"
    color = rgba(201, 199, 205, 1)
    font_size = 80
    font_family = CaskaydiaMono Nerd Font Mono
    position = 0, -180
    halign = center
    valign = top
}

# DAY-DATE-MONTH
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date '+%A, %B %d')</span>"
    color = rgba(201, 199, 205, 1)
    font_size = 30
    font_family = CaskaydiaMono Nerd Font Mono
    position = 0, 200
    halign = center
    valign = center
}

# LOGO
label {
    monitor =
    text =   $USER
    color = rgba(201, 199, 205, 1)
    font_size = 60
    position = 0, 40
    halign = center
    valign = center
}

# USER
# label {
#     monitor =
#     text = Hello, $USER
#     color = rgba(255, 255, 255, .65)
#     font_size = 25
#     # font_family = SF Pro Display Bold
#     font_family = CaskaydiaMono Nerd Font Mono
#     position = 0, -70
#     halign = center
#     valign = center
# }

# INPUT FIELD
input-field {
    monitor =
    size = 290, 60
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(0, 0, 0, 0)
    inner_color = rgba(22, 22, 23, 1)
    font_color = rgba(201, 199, 205, 1)
    fade_on_empty = true
    placeholder_text = Enter Password
    hide_input = false
    position = 0, -140
    halign = center
    valign = center
}
